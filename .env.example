# Configuración de la base de datos MySQL
# Por defecto se conecta a localhost:3306

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=PROYECTO

# Nota: Si usas Docker, cambia:
# DB_HOST=mysql (o el nombre del servicio en docker-compose)
# DB_PASSWORD=tu_contraseña
